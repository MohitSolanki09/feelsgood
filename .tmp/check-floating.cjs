const {chromium}=require('C:/Users/DELL/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{for(const width of[320,360,375,390,414,480,1440]){const p=await b.newPage({viewport:{width,height:900},isMobile:width<768,hasTouch:width<768});await p.goto('http://localhost:3100/',{waitUntil:'networkidle'});const top=p.getByRole('link',{name:'Back to top',exact:true}),wa=p.getByRole('link',{name:'Chat with Feel Good Brass Industry on WhatsApp',exact:true});assert.equal(await top.count(),0);assert.equal(await wa.count(),1);const href=await wa.getAttribute('href'),url=new URL(href);assert.equal(url.pathname,'/919316636271');assert.equal(url.searchParams.get('text'),'Hello Feel Good Brass Industry,\nI would like to inquire about your brass components.');assert.equal(await wa.getAttribute('target'),'_blank');assert.equal(await wa.getAttribute('rel'),'noopener noreferrer');
await p.evaluate(()=>scrollTo(0,450));await p.waitForTimeout(100);assert.equal(await top.count(),0);await p.evaluate(()=>scrollTo(0,1800));await top.waitFor();for(const el of[top,wa]){const r=await el.boundingBox();assert(r.width>=44&&r.height>=44&&r.x>=0&&r.x+r.width<=width&&r.y+r.height<=900)}assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await top.click();await p.waitForTimeout(80);const midway=await p.evaluate(()=>scrollY);assert(midway>0&&midway<1800,'smooth intermediate '+width+' '+midway);await p.waitForFunction(()=>scrollY<=1);assert.equal(await top.count(),0);
await p.getByRole('button',{name:'Open menu',exact:true}).click();await p.waitForTimeout(100);assert.equal(await wa.isVisible(),false);await p.getByRole('button',{name:'Close menu',exact:true}).click();await wa.waitFor({state:'visible'});
await p.evaluate(()=>scrollTo(0,document.documentElement.scrollHeight));await p.waitForTimeout(200);const overlap=await wa.evaluate(a=>{const b=a.parentElement.getBoundingClientRect();return[...document.querySelectorAll('footer a,footer button')].filter(e=>{const r=e.getBoundingClientRect();return r.width&&r.height&&r.left<b.right&&r.right>b.left&&r.top<b.bottom&&r.bottom>b.top}).map(e=>e.textContent.trim())});assert.deepEqual(overlap,[],'footer controls '+width);
if(width===390)await p.screenshot({path:'.tmp/floating-footer.png'});
await p.emulateMedia({reducedMotion:'reduce'});await p.waitForFunction(()=>!document.documentElement.classList.contains('lenis'));await top.click();await p.waitForTimeout(80);assert.equal(await p.evaluate(()=>scrollY),0);console.log('PASS',width,'position, threshold, Lenis smooth scroll, reduced motion, menu, footer, WhatsApp URL');await p.close();}
const p=await b.newPage();await p.goto('http://localhost:3100/contact',{waitUntil:'networkidle'});await p.context().route('https://wa.me/**',route=>route.fulfill({status:200,contentType:'text/html',body:'WhatsApp destination verified'}));const popupPromise=p.waitForEvent('popup');await p.getByRole('link',{name:'Chat with Feel Good Brass Industry on WhatsApp'}).click();const popup=await popupPromise;await popup.waitForLoadState();assert(popup.url().startsWith('https://wa.me/919316636271?'));console.log('PASS WhatsApp new tab (destination intercepted; no message sent)');await p.goto('http://localhost:3100/products/brass-inserts',{waitUntil:'networkidle'});assert.equal(await p.getByRole('link',{name:'Chat with Feel Good Brass Industry on WhatsApp'}).count(),1);console.log('PASS global mount on contact/product detail');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
